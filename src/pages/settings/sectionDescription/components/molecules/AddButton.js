import React from 'react';
import { CustomButton } from '../../../../../components';

const AddButton = ({ props }) => {
    return <CustomButton {...props}>Bölüm Adı Ekle</CustomButton>;
};

export default AddButton;
